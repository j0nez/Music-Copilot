# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
aiofiles>=23.2.0

# Audio Analysis
librosa>=0.10.0
deeprhythm>=0.0.13
# aubio>=0.4.9  # Requires C++ build tools on Windows — add manually if needed

# MIDI
pretty_midi>=0.2.10
mido>=1.3.0
music21>=9.1.0

# HTTP (for AI providers)
httpx>=0.27.0

# Audio Generation (tests only, pulled by librosa but pinned for clarity)
scipy>=1.12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
